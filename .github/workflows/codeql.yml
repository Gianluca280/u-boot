name: "CodeQL"

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      security-events: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: c-cpp
        queries: +./.github/codeql/queries/taint_analysis.ql
        
    - name: Build U-Boot
      run: |
        sudo apt-get update
        sudo apt-get install -y libsdl2-dev build-essential device-tree-compiler bison flex
        
        # Pulizia completa per evitare residui dei tentativi falliti
        make distclean
        make sandbox_defconfig
        
        # Esportiamo i flag per il compilatore host
        export HOSTCFLAGS="-fcommon"
        export KBUILD_HOSTCFLAGS="-fcommon"
        
        # Lanciamo la build forzando i flag e disabilitando SDL
        make -j$(nproc) HOSTCFLAGS="$HOSTCFLAGS" KBUILD_HOSTCFLAGS="$KBUILD_HOSTCFLAGS" NO_SDL=1

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:c-cpp"
